# CodeRipple配置文件

# 商用版本仓库地址（可选）
# commercial_repo: "git@github.com:company/product-release.git"

# 缓存路径（可选，默认：~/.coderipple/cache）
# cache_path: "~/.coderipple/cache"

# 相似度阈值（可选，默认：0.85）
# similarity_threshold: 0.85

# 日志级别（可选，默认：INFO）
# log_level: "INFO"
